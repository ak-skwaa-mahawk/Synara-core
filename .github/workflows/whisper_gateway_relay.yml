# 🔕 Whisper Gateway Relay - Disabled for now
on:
  workflow_dispatch:  # Manual trigger only

name: Whisper Gateway Relay

jobs:
  whisper-bump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: 🔕 Pause Log
        run: echo "Whisper Gateway Relay is in paused/manual mode. No jobs are currently active."